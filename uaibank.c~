#include <stdio.h>

typedef struct {
	int id;
	char name[100];
	int age;
	float current_balance;
} User;

void main() {
  User user[100];
  char name[100];
  int age;
  float current_balance;

  printf("Digite o nome do usuário: ");
  scanf("%[^\n]", &user[0].name);

  // user.name = name;
 
  printf("usuário: %s\n", user[0].name);
  
  return;
}

User create_user(int users_qnty) {
}
